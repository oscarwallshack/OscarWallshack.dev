import React from 'react';
import TopNavigation from './Navigation/TopNavigation';


const Header = (props) => {
  return (
    <header >
    <TopNavigation />
    </header>
  );
};

export default Header;
