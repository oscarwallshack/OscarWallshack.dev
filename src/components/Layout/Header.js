import React from 'react';
import Hamburger from '../UI/Hamburger/Hamburger';


const Header = (props) => {
  return (
    <header >
      <nav>
      <a href="">abc</a>
      <Hamburger />
      </nav>
    </header>
  );
};

export default Header;
